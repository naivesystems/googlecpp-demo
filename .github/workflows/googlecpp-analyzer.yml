name: Run static analysis with Google C++ Analyzer
on: [push]

jobs:
  static-analysis:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Build Google C++ Analyzer
        uses: naivesystems/googlecpp@main
        with:
          cwd: $GITHUB_WORKSPACE
